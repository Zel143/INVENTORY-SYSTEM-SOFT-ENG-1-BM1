# 📊 StockSense Firebase Architecture - Visual Guide

## System Overview Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                       STOCKSENSE CLOUD                          │
│                    Inventory Management System                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │
        ┌──────────────────────┴──────────────────────┐
        │                                             │
   ┌────▼─────┐                               ┌──────▼──────┐
   │  ADMIN   │                               │    STAFF    │
   │  Portal  │                               │   Terminal  │
   └────┬─────┘                               └──────┬──────┘
        │                                             │
        │                                             │
        └─────────────────┬───────────────────────────┘
                          │
                    ┌─────▼──────┐
                    │  Firebase  │
                    │    Auth    │ (Future: Currently Session-based)
                    └─────┬──────┘
                          │
                ┌─────────┴─────────┐
                │                   │
         ┌──────▼───────┐    ┌─────▼──────┐
         │  Web Client  │    │ Web Client │
         │  (Browser)   │    │ (Browser)  │
         └──────┬───────┘    └─────┬──────┘
                │                   │
                │  Real-time Sync   │
                │  (<2 seconds)     │
                │                   │
         ┌──────▼───────────────────▼──────┐
         │    Firebase Cloud Firestore     │
         │       (NoSQL Database)          │
         └──────┬───────────────────┬──────┘
                │                   │
     ┌──────────▼────────┐  ┌──────▼───────────────┐
     │  Security Rules   │  │  Offline Persistence │
     │  (Server-side)    │  │  (Local Cache)       │
     └───────────────────┘  └──────────────────────┘
```

---

## Three-Collection Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    FIRESTORE DATABASE                           │
└─────────────────────────────────────────────────────────────────┘
                               │
            ┌──────────────────┼──────────────────┐
            │                  │                  │
      ┌─────▼─────┐    ┌───────▼────────┐  ┌─────▼─────────┐
      │ inventory │    │ transactions   │  │allocation_logs│
      │           │    │                │  │               │
      │ MCH-001 ──┼───▶│ Auto-ID-1      │  │ Auto-ID-A     │
      │ MCH-002   │    │ Auto-ID-2      │  │ Auto-ID-B     │
      │ EQP-104   │    │ Auto-ID-3      │  │ Auto-ID-C     │
      │ STR-201   │    │     ...        │  │     ...       │
      └───────────┘    └────────────────┘  └───────────────┘
           │                   │                    │
           │                   │                    │
    ┌──────▼──────┐    ┌───────▼────────┐   ┌──────▼────────┐
    │ Document:   │    │ Document:      │   │ Document:     │
    │ MCH-001     │    │ xyz123         │   │ abc456        │
    ├─────────────┤    ├────────────────┤   ├───────────────┤
    │description  │    │item_id (ref)   │   │request_id     │
    │vendor       │    │item_name       │   │item_id (ref)  │
    │current_stock│    │actor_id        │   │quantity       │
    │allocated_   │    │quantity_change │   │requestor      │
    │  stock      │    │timestamp       │   │status         │
    │min_threshold│    │destination     │   │created_at     │
    │max_ceiling  │    │purpose         │   │approved_by    │
    │warranty_*   │    └────────────────┘   └───────────────┘
    │location     │    
    │image        │    
    └─────────────┘    

    READ: All          READ: Admin only    READ: All
    WRITE: Staff       WRITE: All          WRITE: Admin only
    (stock only)       UPDATE: DENIED ❌   (MA management)
    Admin: All fields  DELETE: DENIED ❌
```

---

## Data Flow: Stock Update Transaction

```
USER ACTION: "Dispatch 3 units of MCH-001 to Production Floor"

Step 1: Click "Dispatch" button
   │
   ▼
Step 2: Fill transaction form
   │   ├─ Quantity: -3
   │   ├─ Destination: "Production Floor A"
   │   └─ Purpose: "Assembly Line #2"
   │
   ▼
Step 3: Submit form
   │
   ├──────────────────┬──────────────────┐
   │                  │                  │
   ▼                  ▼                  ▼
┌──────────┐   ┌─────────────┐   ┌─────────────┐
│Security  │   │Update       │   │Create       │
│Rules     │   │inventory    │   │transaction  │
│Check     │   │MCH-001      │   │record       │
│          │   │             │   │             │
│✓ User    │   │current_stock│   │{            │
│  authed  │   │  = 5 - 3    │   │ actor_id    │
│✓ Has     │   │  = 2        │   │ item_name   │
│  role    │   │             │   │ change: -3  │
│✓ Can     │   │(ALLOWED)    │   │ dest: "..." │
│  write   │   │             │   │}            │
│  stock   │   │             │   │(IMMUTABLE)  │
└────┬─────┘   └──────┬──────┘   └──────┬──────┘
     │                │                 │
     │                │                 │
     └────────────────┴─────────────────┘
                      │
                      ▼
              ┌───────────────┐
              │ onSnapshot()  │
              │ Listener      │
              │ Triggered     │
              └───────┬───────┘
                      │
                      ▼
            ┌─────────────────────┐
            │  All Connected Tabs │
            │  Update in <2 sec   │
            └─────────────────────┘
```

---

## Security Rules Logic Flow

```
SCENARIO: Staff tries to change min_threshold

User: staff@stocksense.com
Action: Update inventory/MCH-001
Field: min_threshold = 3 (lowering from 5)

┌─────────────────────────────────────┐
│  FIRESTORE SECURITY RULES ENGINE    │
└─────────────────────────────────────┘
              │
              ▼
    ┌─────────────────┐
    │ Is user authed? │
    │   (logged in)   │
    └────────┬────────┘
             │ YES
             ▼
    ┌─────────────────┐
    │ Check rule for  │
    │ inventory/{item}│
    └────────┬────────┘
             │
             ▼
    ┌────────────────────────────────────┐
    │ allow update: if isAuthenticated() │
    │   && (                             │
    │   request.resource.data.diff(      │
    │     resource.data).affectedKeys()  │
    │     .hasOnly(['current_stock',     │
    │                'allocated_stock']) │
    │   || isAdmin()                     │
    │   )                                │
    └────────┬───────────────────────────┘
             │
             ▼
   ┌─────────────────────┐
   │ What fields changed?│
   └────────┬────────────┘
            │
            ▼
   Changed: ['min_threshold']
            │
            ▼
   ┌──────────────────────────┐
   │ Is 'min_threshold' in    │
   │ ['current_stock',        │
   │  'allocated_stock']?     │
   └────────┬─────────────────┘
            │ NO
            ▼
   ┌──────────────────┐
   │ Is user admin?   │
   │ isAdmin()        │
   └────────┬─────────┘
            │
            ▼
    Query users/staff_001
            │
            ▼
    role = "staff"
            │
            │ NO (not admin)
            ▼
   ┌────────────────────┐
   │ ❌ PERMISSION      │
   │    DENIED          │
   └────────────────────┘
            │
            ▼
   Client receives error:
   "Missing or insufficient permissions"
```

---

## BEFORE vs AFTER Architecture

### BEFORE: localStorage (Single Device)
```
┌──────────────────┐
│  Browser Tab 1   │
│                  │
│  ┌────────────┐  │
│  │ localStorage │  │
│  │  (5MB max)  │  │
│  └────────────┘  │
│                  │
│  app.js (vanilla)│
└──────────────────┘
       │
       │ NO SYNC
       │
       ▼
  (Data isolated)


┌──────────────────┐
│  Browser Tab 2   │
│                  │
│  ┌────────────┐  │
│  │ localStorage │  │
│  │ (different) │  │
│  └────────────┘  │
│                  │
└──────────────────┘

❌ No synchronization
❌ No audit trail
❌ No role-based security
❌ Data loss on browser clear
```

### AFTER: Firebase Cloud Firestore
```
┌──────────────────┐      ┌──────────────────┐
│  Browser Tab 1   │      │  Browser Tab 2   │
│  (Admin view)    │      │  (Staff view)    │
│                  │      │                  │
│  app-firebase.js │      │  app-firebase.js │
│  onSnapshot()    │      │  onSnapshot()    │
└────────┬─────────┘      └────────┬─────────┘
         │                         │
         │   Real-time Sync        │
         │   (<2 seconds)          │
         └────────┬────────────────┘
                  │
         ┌────────▼────────┐
         │  FIRESTORE DB   │
         │  (Cloud)        │
         │                 │
         │ ┌─────────────┐ │
         │ │ inventory   │ │
         │ ├─────────────┤ │
         │ │transactions │ │
         │ ├─────────────┤ │
         │ │allocation_  │ │
         │ │  logs       │ │
         │ └─────────────┘ │
         │                 │
         │ Security Rules  │
         │ (Server-side)   │
         └─────────────────┘

✅ Real-time sync across devices
✅ Immutable audit trail
✅ Role-based access control
✅ Unlimited storage
✅ Offline persistence
✅ Server-side validation
```

---

## Real-Time Sync Mechanism (Melprin's Domain)

```
┌─────────────────────────────────────────────────────────────┐
│              WebSocket Connection (Persistent)              │
└─────────────────────────────────────────────────────────────┘
                              │
                              │
        ┌─────────────────────┴─────────────────────┐
        │                                           │
   ┌────▼──────┐                              ┌────▼──────┐
   │ Client A  │                              │ Client B  │
   │ (Admin)   │                              │ (Staff)   │
   └────┬──────┘                              └────┬──────┘
        │                                           │
        │ 1. Update inventory/MCH-001               │
        │    current_stock = 10                     │
        ▼                                           │
   ┌────────────────┐                               │
   │ FIRESTORE DB   │───────────────────────────────┘
   │                │    2. Push notification
   │ inventory/     │       "MCH-001 changed"
   │   MCH-001      │
   │   stock = 10   │
   └────────────────┘
        │
        │ 3. onSnapshot() callback fires
        ▼
   ┌────────────────┐
   │ Client B's     │
   │ UI Updates     │ 4. Re-render table (< 2 seconds)
   │ Automatically  │
   └────────────────┘

CODE IMPLEMENTATION (app-firebase.js):

unsubscribeInventory = inventoryRef.onSnapshot((snapshot) => {
    inventoryCache = [];
    snapshot.forEach((doc) => {
        inventoryCache.push({ code: doc.id, ...doc.data() });
    });
    renderAll(); // ← Triggers UI update
});

✨ No polling, no manual refresh, pure event-driven architecture
```

---

## Immutable Transaction "Black Box"

```
NORMAL FIRESTORE DOCUMENT:
┌──────────────────────┐
│ inventory/MCH-001    │
├──────────────────────┤
│ description: "..."   │ ← Can be updated
│ current_stock: 10    │ ← Can be updated
│ min_threshold: 5     │ ← Can be updated (admin only)
└──────────────────────┘

        🔄 Mutable


IMMUTABLE TRANSACTION DOCUMENT:
┌──────────────────────┐
│ transactions/xyz123  │
├──────────────────────┤
│ actor_id: "admin001" │ ← CANNOT be updated
│ quantity_change: -3  │ ← CANNOT be updated
│ timestamp: 1234567   │ ← CANNOT be updated
│ destination: "..."   │ ← CANNOT be updated
└──────────────────────┘

        🔒 Immutable (Black Box)


SECURITY RULE ENFORCEMENT:

match /transactions/{id} {
  allow create: if isAuthenticated();  ✅ Anyone can create
  allow read: if isAdmin();            ✅ Admin can read
  allow update: if false;              ❌ NO ONE can update
  allow delete: if false;              ❌ NO ONE can delete
}

ATTEMPTS TO MODIFY:
┌──────────────────────────────────────────────┐
│ Admin tries: transactions.doc(xyz).update({  │
│   quantity_change: -5  // Changed from -3   │
│ })                                           │
│                                              │
│ Result: ❌ PERMISSION_DENIED                 │
│ "Missing or insufficient permissions"        │
└──────────────────────────────────────────────┘

WHY THIS MATTERS:
1. Audit Integrity: No retroactive changes to history
2. Compliance: Satisfies SOX/GDPR audit requirements
3. Trust: Management can trust warehouse data
4. Accountability: Cannot hide mistakes by editing logs
```

---

## Role-Based Access Matrix

```
┌────────────────────┬──────────┬──────────┬───────────────┐
│ OPERATION          │  ADMIN   │  STAFF   │  EXPLANATION  │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Read inventory     │    ✅    │    ✅    │ All can view  │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Update stock       │    ✅    │    ✅    │ Operational   │
│ (current_stock)    │          │          │ necessity     │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Update thresholds  │    ✅    │    ❌    │ Prevents      │
│ (min/max)          │          │          │ threshold     │
│                    │          │          │ gaming        │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Read transactions  │    ✅    │    ❌    │ Black Box     │
│ (audit trail)      │          │          │ isolation     │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Create transaction │    ✅    │    ✅    │ All stock     │
│                    │          │          │ changes       │
│                    │          │          │ logged        │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Update transaction │    ❌    │    ❌    │ IMMUTABLE     │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Delete transaction │    ❌    │    ❌    │ IMMUTABLE     │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Write allocation_  │    ✅    │    ❌    │ MA tracking   │
│ logs               │          │          │ admin-only    │
├────────────────────┼──────────┼──────────┼───────────────┤
│ Manage user roles  │    ✅    │    ❌    │ Admin         │
│                    │          │          │ governance    │
└────────────────────┴──────────┴──────────┴───────────────┘

SECURITY PRINCIPLE: Principle of Least Privilege
- Staff gets minimum permissions needed for daily operations
- Admin controls governance parameters (thresholds, roles)
- NO ONE (including admins) can alter audit trail
```

---

## Offline Persistence Flow

```
SCENARIO: Warehouse loses internet connection

Time: 10:00 AM
┌──────────────────┐
│  Staff Device    │
│                  │
│ 📶 Connected     │
│                  │
│ Sync: Active     │
└──────────────────┘
       │
       ▼
┌─────────────────────┐
│ FIRESTORE CLOUD DB  │
│ (Live connection)   │
└─────────────────────┘

- - - - - - - - - - - - - - - - - - - - - - - - - -

Time: 10:05 AM - Internet outage

┌──────────────────┐
│  Staff Device    │
│                  │
│ ❌ Offline       │
│                  │
│ ┌──────────────┐ │
│ │ Local Cache  │ │  ← IndexedDB storage
│ │ (Firebase    │ │     (Enabled via enablePersistence)
│ │  Persistence)│ │
│ │              │ │
│ │ inventory    │ │
│ │   MCH-001    │ │
│ │   MCH-002    │ │
│ │              │ │
│ │ PENDING:     │ │
│ │ +5 to MCH-001│ │  ← Queued writes
│ └──────────────┘ │
└──────────────────┘

Staff can:
✅ View inventory (from cache)
✅ Add/remove stock (queued locally)
✅ Search items (local data)

Staff cannot:
❌ See real-time updates from other devices
❌ Sync immediately to cloud

- - - - - - - - - - - - - - - - - - - - - - - - - -

Time: 10:20 AM - Internet restored

┌──────────────────┐
│  Staff Device    │
│                  │
│ 📶 Re-connected  │
│                  │
│ ┌──────────────┐ │
│ │ Sync Queue   │ │
│ │              │ │
│ │ 1. +5 MCH-001│ ├──┐
│ │ 2. -2 EQP-104│ │  │
│ └──────────────┘ │  │
└──────────────────┘  │
                      │
                      ▼
             ┌─────────────────┐
             │ FIRESTORE CLOUD │
             │                 │
             │ Applying queued │
             │ writes...       │
             │                 │
             │ ✅ Success!     │
             └─────────────────┘
                      │
                      ▼
             All devices sync with
             latest changes

CODE IMPLEMENTATION (firebase-config.js):

db.enablePersistence()
  .then(() => console.log("✓ Offline persistence enabled"))
  .catch((err) => {
    if (err.code === 'failed-precondition') {
      console.warn("⚠️ Offline persistence: Multiple tabs open");
    }
  });

✨ Zero code changes needed - Firebase handles sync automatically!
```

---

## Team Collaboration Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    STOCKSENSE TEAM                          │
└─────────────────────────────────────────────────────────────┘
                              │
            ┌─────────────────┼─────────────────┐
            │                 │                 │
       ┌────▼────┐      ┌─────▼──────┐    ┌────▼─────┐
       │  YOU    │      │  Melprin   │    │ Raphael  │
       │         │      │            │    │          │
       └────┬────┘      └─────┬──────┘    └────┬─────┘
            │                 │                 │
            │                 │                 │
    ┌───────▼────────┐ ┌──────▼────────┐ ┌────▼──────────┐
    │ Frontend       │ │ Real-time     │ │ Cloud         │
    │                │ │ Sync          │ │ Functions     │
    ├────────────────┤ ├───────────────┤ ├───────────────┤
    │• dashboard.html│ │• onSnapshot() │ │• Daily        │
    │• app-firebase  │ │  listeners    │ │  summaries    │
    │  .js           │ │• WebSocket    │ │• Auto email   │
    │• UI/UX         │ │  management   │ │  alerts       │
    │• Transaction   │ │• Latency      │ │• MA approval  │
    │  forms         │ │  optimization │ │  workflow     │
    │• Search/filter │ │               │ │               │
    └────────┬───────┘ └───────┬───────┘ └───────┬───────┘
             │                 │                 │
             │                 │                 │
             └─────────────────┼─────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   FIRESTORE DB      │
                    │                     │
                    │ ┌─────────────────┐ │
                    │ │ inventory       │ │ ← YOU & Melprin
                    │ ├─────────────────┤ │
                    │ │ transactions    │ │ ← YOU (creates)
                    │ ├─────────────────┤ │
                    │ │ allocation_logs │ │ ← Raphael
                    │ ├─────────────────┤ │
                    │ │ daily_summaries │ │ ← Raphael
                    │ └─────────────────┘ │
                    │                     │
                    │ ┌─────────────────┐ │
                    │ │ firestore.rules │ │ ← YOU (defines)
                    │ └─────────────────┘ │
                    └─────────────────────┘

COLLABORATION INTERFACE (Collection Schema):

inventory/{itemCode}
├─ description: string
├─ current_stock: number      ← YOU update
├─ allocated_stock: number    ← Raphael updates
└─ min_threshold: number      ← YOU set (admin)

transactions/{autoId}
├─ item_id: reference         ← YOU create
├─ quantity_change: number    ← YOU populate
└─ timestamp: timestamp       ← Auto-generated

allocation_logs/{autoId}
├─ item_id: reference         ← Raphael creates
├─ requestor: string          ← Raphael
└─ status: string             ← Raphael workflow

COMMUNICATION PROTOCOL:
• YOU define security rules → Raphael follows
• YOU create transactions → Raphael reads for reports
• Raphael updates allocated_stock → YOU display in UI
• All changes real-time via Melprin's onSnapshot()
```

---

## For Professor: Key Selling Points

### 1. Cloud-Native Architecture
```
Traditional (Client-Server):
Client ←→ API Server ←→ Database
(3 hops, custom backend code)

StockSense (Serverless):
Client ←→ Firestore
(1 hop, declarative rules)

✅ Reduced latency
✅ No backend maintenance
✅ Auto-scaling
```

### 2. Security-First Design
```
Client-side validation:
❌ Can be bypassed (edit HTML)

Server-side rules (StockSense):
✅ Cannot be bypassed
✅ Enforced at database level
✅ Auditable rule code
```

### 3. Real-World Scalability
```
localStorage:
❌ 5-10MB limit
❌ Single device
❌ Manual sync

Firebase Firestore:
✅ Unlimited storage
✅ Multi-device auto-sync
✅ Global CDN
✅ Used by: Duolingo, Alibaba, The New York Times
```

### 4. Production-Ready
```
Missing from typical student projects:
❌ Error handling
❌ Offline support
❌ Audit trails
❌ Role-based security

StockSense includes:
✅ Try-catch blocks
✅ enablePersistence()
✅ Immutable transactions
✅ Declarative security rules
```

---

**🎓 Use this document during your March 21 presentation to visually explain the architecture to your professor!**

---

**Tip**: Print this document and annotate it during your demo to show you truly understand the system architecture, not just copied code.