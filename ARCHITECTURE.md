# 🏗️ StockSense Architecture Diagram

## Before Firebase (localStorage)

```
┌──────────────────────────────────────────────────────┐
│                   Browser Tab 1                      │
│  ┌────────────┐    ┌─────────────┐   ┌────────────┐│
│  │ index.html │───▶│  app.js     │──▶│localStorage││
│  └────────────┘    └─────────────┘   └────────────┘│
│       ▲                  │                           │
│       │                  ▼                           │
│   [Login]           [Render UI]                     │
└──────────────────────────────────────────────────────┘
         ❌ NO SYNC BETWEEN TABS ❌
┌──────────────────────────────────────────────────────┐
│                   Browser Tab 2                      │
│  ┌────────────┐    ┌─────────────┐   ┌────────────┐│
│  │ index.html │───▶│  app.js     │──▶│localStorage││
│  └────────────┘    └─────────────┘   └────────────┘│
│   (Different data - manual refresh required)        │
└──────────────────────────────────────────────────────┘
```

**Problems:**
- ❌ Data stuck on one device
- ❌ No real-time updates
- ❌ Manual refresh needed
- ❌ Team can't collaborate
- ❌ 168-hour information lag

---

## After Firebase (Cloud Firestore)

```
┌──────────────────────────────────────────────────────┐
│                   Browser Tab 1                      │
│  ┌────────────┐    ┌──────────────────────────────┐ │
│  │ index.html │───▶│    dashboard.html            │ │
│  └────────────┘    │  + firebase-config.js        │ │
│       ▲            │  + onSnapshot() listeners    │ │
│    [Login]         └──────────────────────────────┘ │
│                              │                       │
│                              ▼                       │
│                    [Real-time Updates]               │
└──────────────────────────────│───────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    ▼                     ▼
        ╔═══════════════════════════════════════╗
        ║      FIREBASE CLOUD FIRESTORE          ║
        ║                                        ║
        ║  📁 inventory/{itemCode}              ║
        ║     ├─ description                    ║
        ║     ├─ current_stock                  ║
        ║     ├─ allocated_stock                ║
        ║     └─ min_threshold                  ║
        ║                                        ║
        ║  📁 transactions/{id}                 ║
        ║     ├─ item_id (reference)            ║
        ║     ├─ quantity_change                ║
        ║     ├─ timestamp                      ║
        ║     └─ destination                    ║
        ║                                        ║
        ║  📁 allocation_logs/{id}              ║
        ║     ├─ item_id (reference)            ║
        ║     ├─ reserved_quantity              ║
        ║     └─ status (Active/Released)       ║
        ║                                        ║
        ║  🔒 Security Rules (firestore.rules)  ║
        ║     ├─ Admin: Full access             ║
        ║     └─ Staff: Limited access          ║
        ╚═══════════════════════════════════════╝
                    ▲                     ▲
                    │                     │
        ┌───────────┴──────────┐          │
        │                      │          │
┌───────┴──────────────┐  ┌────┴──────────────┐
│   Browser Tab 2      │  │   Mobile Device    │
│  dashboard.html      │  │  (Future: PWA)     │
│  ✅ Instant sync!    │  │  ✅ Real-time too!│
└──────────────────────┘  └────────────────────┘
```

**Benefits:**
- ✅ Real-time sync (<2 seconds)
- ✅ Works on all devices
- ✅ Offline persistence
- ✅ Team collaboration
- ✅ Eliminates 168-hour lag

---

## Data Flow: Adding Stock

### Old Way (localStorage)
```
User clicks +
    ↓
app.js updates local array
    ↓
localStorage.setItem()
    ↓
renderAll()
    ↓
[END] - Other tabs DON'T know about this!
```

### New Way (Firebase)
```
User clicks +
    ↓
dashboard.html calls Firebase
    ↓
inventoryRef.doc(code).update()
    ↓
Firestore writes to cloud ☁️
    ↓
[AUTOMATIC] onSnapshot() triggers on ALL tabs
    ↓
renderAll() on every connected device
    ↓
[END] - Everyone sees update in <2 seconds! ⚡
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────┐
│            User Attempts Action                 │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
         ┌─────────────────────┐
         │  Firebase Auth      │
         │  (Future upgrade)   │
         │  Currently: session │
         └─────────┬───────────┘
                   │
                   ▼
         ┌─────────────────────┐
         │ Firestore Rules     │ ← firestore.rules
         │ Check permissions   │
         └─────────┬───────────┘
                   │
         ┌─────────┴─────────┐
         ▼                   ▼
    [ALLOWED]           [DENIED]
         │                   │
         ▼                   ▼
  Write to DB         Show error
  Update all tabs     Permission denied
```

### Example: Staff tries to change min_threshold

```
1. Staff user clicks "Edit threshold"
2. Firebase Security Rules evaluate:
   - Is user authenticated? ✅ YES
   - Is user admin? ❌ NO (user is staff)
3. Rule: allow update: if isAdmin()
4. RESULT: ❌ PERMISSION DENIED
5. Staff sees: "Only admins can change thresholds"
```

### Example: Admin changes min_threshold

```
1. Admin user clicks "Edit threshold"
2. Firebase Security Rules evaluate:
   - Is user authenticated? ✅ YES
   - Is user admin? ✅ YES
3. Rule: allow update: if isAdmin()
4. RESULT: ✅ ALLOWED
5. Firestore updates document
6. All tabs see new threshold value instantly
```

---

## Team Implementation Map

```
┌─────────────────────────────────────────────────────┐
│                   FRONT-END                         │
│  ┌──────────────────────────────────────────────┐  │
│  │  dashboard.html (Melprin's Focus)            │  │
│  │  • onSnapshot() listeners                    │  │
│  │  • Real-time UI updates                      │  │
│  │  • Offline persistence handling              │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│              FIREBASE SERVICES                      │
│  ┌──────────────────────────────────────────────┐  │
│  │  Cloud Firestore (Data Layer)               │  │
│  │  • Collections: inventory, transactions      │  │
│  │  • Real-time listeners                       │  │
│  │  • Offline cache                             │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │  Security Rules (Arthur's Focus)             │  │
│  │  • Role-based access control                 │  │
│  │  • Field-level permissions                   │  │
│  │  • Immutable audit trails                    │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │  Cloud Functions (Raphael's Focus)           │  │
│  │  • Burn rate calculation                     │  │
│  │  • Allocated stock aggregation               │  │
│  │  • Daily summaries                           │  │
│  │  • Automated alerts                          │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

---

## Transaction Flow (Deep Traceability)

```
User dispatches 5 Forklifts to Department A
                ↓
┌───────────────────────────────────────────────┐
│  1. UPDATE: inventory/MCH-001                │
│     current_stock: 10 → 5                    │
│     (Protected: can't go below allocated)     │
└───────────────────────────────────────────────┘
                ↓
┌───────────────────────────────────────────────┐
│  2. CREATE: transactions/{auto-id}           │
│     item_id: ref(inventory/MCH-001)          │
│     quantity_change: -5                      │
│     timestamp: 2026-02-12T10:30:00Z          │
│     destination: "Department A"              │
│     purpose: "MA-2026-001"                   │
│     actor_id: "staff_john"                   │
└───────────────────────────────────────────────┘
                ↓
┌───────────────────────────────────────────────┐
│  3. TRIGGER: onSnapshot() listeners          │
│     • Tab 1: Updates inventory table         │
│     • Tab 2: Updates inventory table         │
│     • History view: Adds transaction row     │
│     • Alerts: Checks if below threshold      │
└───────────────────────────────────────────────┘

Result: Complete audit trail!
- ✅ Where: Department A
- ✅ When: 2026-02-12T10:30:00Z
- ✅ Who: staff_john
- ✅ Why: MA-2026-001
- ✅ How much: -5 units
- ✅ Immutable: Can't be deleted
```

---

## Offline Resilience Architecture

```
┌─────────────────────────────────────────────┐
│      Warehouse Staff (Dead Zone)            │
│  ┌───────────────────────────────────────┐ │
│  │  dashboard.html                       │ │
│  │  [OFFLINE] No internet connection     │ │
│  └───────────────────────────────────────┘ │
│                   │                         │
│                   ▼                         │
│  ┌───────────────────────────────────────┐ │
│  │  Firebase Offline Cache               │ │
│  │  • Stores transaction locally         │ │
│  │  • Queues for sync                    │ │
│  │  • UI shows pending status            │ │
│  └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
              [Internet restored]
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Firebase Automatic Sync                    │
│  1. Detect connection                       │
│  2. Upload queued transactions              │
│  3. Resolve conflicts (if any)              │
│  4. Sync complete ✅                        │
└─────────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  All Devices Updated                        │
│  • Office sees warehouse changes            │
│  • Other staff see updates                  │
│  • No "phantom stock" errors                │
└─────────────────────────────────────────────┘
```

**Key Feature:** Prevents "phantom stock" where multiple people think they have available inventory, but it's already allocated elsewhere.

---

## Comparison Table

| Feature | localStorage | Firebase Firestore |
|---------|-------------|-------------------|
| **Sync Speed** | Manual refresh | <2 seconds |
| **Multi-device** | ❌ No | ✅ Yes |
| **Offline Support** | ✅ Yes (local only) | ✅ Yes (auto-sync) |
| **Security** | Client-side only | Server-side rules |
| **Scalability** | Limited to device | Unlimited users |
| **Audit Trail** | Can be deleted | Immutable |
| **Team Collaboration** | ❌ No | ✅ Yes |
| **Setup Complexity** | Easy | Moderate |
| **Cost** | Free | Free (up to 50K reads/day) |
| **Deployment** | Just open file | Requires Firebase account |

---

## File Relationship Diagram

```
index.html (Login)
    │
    ├─ style.css (styling)
    │
    └─ [Login Success] ─────────▶ dashboard.html
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
                    ▼                  ▼                  ▼
              dashboard.css     firebase-config.js   (old) app.js
              (styling)          (connection)        (not used)
                                       │
                                       ▼
                              Firebase Cloud ☁️
                                       │
                    ┌──────────────────┼──────────────────┐
                    ▼                  ▼                  ▼
              firestore.rules   functions-example.js  DOCS
              (security)        (Raphael's work)     (your guides)
```

---

## This Architecture Solves:

1. ✅ **168-Hour Lag** → Real-time sync (<2 seconds)
2. ✅ **Phantom Stock** → Offline persistence + conflict resolution
3. ✅ **Accountability** → Immutable audit trail
4. ✅ **Unauthorized Changes** → Security rules
5. ✅ **Manual Tracking** → Automated "Saan Napunta" logging
6. ✅ **Scalability** → Cloud infrastructure
7. ✅ **Team Collaboration** → Multi-device sync

---

**Ready to see this in action?** Follow [QUICK_START.md](QUICK_START.md)!
