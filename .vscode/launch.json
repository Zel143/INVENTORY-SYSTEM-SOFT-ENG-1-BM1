{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run StockSense Server",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/server.js",
      "runtimeExecutable": "${workspaceFolder}/node/node.exe",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "restart": false
    }
  ]
}
